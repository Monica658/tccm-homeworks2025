## How to compile and run the program

This document contains the instructions on how to compile and run the Molecular Dynamics simulation program.

### Prerequisites

To compile and run this project, you need:
* A Fortran compiler (`gfortran`).
* To compile 

```bash
gfortran -o name_executable dynamics.f90
```

* To execute the executable

```bash
./name_executable
```
